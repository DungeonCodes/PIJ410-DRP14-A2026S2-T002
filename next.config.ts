import type { NextConfig } from "next";

// -----------------------------------------------------------------------------
// Aplicação acadêmica do PIJ410. Sem rewrites, sem proxies, sem integrações.
// Não há variáveis de ambiente obrigatórias: TODO dado é sintético e vem de
// arquivos JSON versionados dentro de `src/data/`.
// -----------------------------------------------------------------------------
const nextConfig: NextConfig = {};

export default nextConfig;
